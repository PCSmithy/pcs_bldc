{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC/events plus the file-open dialog for the .elf picker.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
