#pragma once

/* Includes */

/* Defines */

// Motor bridges on the pcs_bldc board. One today (the single motor bridge).
typedef enum
{
    IO_BRIDGE_CHANNEL_MOTOR,
    IO_BRIDGE_CHANNEL_COUNT,
} IO_bridge_channel_E;
