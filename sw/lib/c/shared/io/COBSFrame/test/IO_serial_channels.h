#pragma once

// Test-local channel seam for IO_serial, compiled into the IO_COBSFrame unit
// suite (stands in for the project's IO_serial_channels.h).

/* Typedefs */

typedef enum
{
    IO_SERIAL_CHANNEL_CDC,
    IO_SERIAL_CHANNEL_COUNT,
} IO_serial_channel_E;
