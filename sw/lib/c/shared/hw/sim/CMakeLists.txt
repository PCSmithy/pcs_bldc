# Shared simulation-support modules (native sim target only). These are the
# generic sim utilities the per-peripheral sim drivers lean on — not
# channelized (no per-target split: they exist only on native).

add_subdirectory(ports)
